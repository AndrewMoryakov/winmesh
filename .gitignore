# Your real fleet config — everyone has their own, keep it out of the repo.
config/hosts.psd1

# Credential store. DPAPI files are useless on another machine, but there is no
# reason to risk it — keep it out of git.
.winmesh/
*.cred.xml

# Generated per-machine bootstrap scripts.
bootstrap-*.ps1
