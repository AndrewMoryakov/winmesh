# Реальный конфиг парка — у каждого свой, в репозиторий не кладём.
config/hosts.psd1

# Хранилище учётных данных. DPAPI-файлы бесполезны на другой машине, но незачем
# и рисковать — держим вне git.
.winmesh/
*.cred.xml

# Сгенерированные bootstrap-скрипты для конкретных машин.
bootstrap-*.ps1
